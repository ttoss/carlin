/** @jsx jsx */
import { jsx } from 'theme-ui';

const Header = () => {
  return (
    <header
      sx={{
        height: '100px',
        backgroundColor: 'black',
      }}
    >
      Carlin
    </header>
  );
};

export default Header;
