skipDeploy: true
environments:
  Production:
    aliases:
      - carlin.ttoss.dev
    csp:
      default-src: ["* 'unsafe-inline' 'unsafe-eval'", replace]
    cloudfront: true
    skipDeploy: false
    stackName: CarlinWebsite
