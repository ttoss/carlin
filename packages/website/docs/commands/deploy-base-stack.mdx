---
title: deploy base-stack
---

import InnerHTML from '../../src/components/InnerHTML';
import Template from '../../src/components/Template';

import { baseStackTemplate } from '../../.docusaurus/carlin/default/baseStackTemplate';
import { deployBaseStackComment } from '../../.docusaurus/carlin/default/deployBaseStackComment';

<InnerHTML html={deployBaseStackComment} />

## Deploying Base Stack

```sh
carlin deploy base-stack
```

## Template

The command create a stack whose template is:

<Template json={baseStackTemplate} />
